machine:
  node:
    version: '0.12.0'
dependencies:
  override:
    - npm install
    - bower install
    - gem install scss-lint
    - npm run compile
    - aws s3 sync dist/* s3://namely-styleguid/`echo $CIRCLE_SHA1 | cut -c1-8`/ --acl public-read 

test:
  override:
    - echo "TODO"


machine:
  node:
    version: '0.12.0'
dependencies:
  override:
    - npm install
    - bower install
    - gem install scss-lint
    - npm run compile
    - echo "circle test" > test.txt
    - aws s3 cp test.txt s3://namely-styleguide/test.txt

test:
  override:
    - echo "TODO"

